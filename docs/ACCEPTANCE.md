# 验收记录

验收日期：2026-08-29

## V1.1.0 功能与升级验收

### 自动化与安全

- Flutter 静态分析：零问题。
- 自动化测试：13 项全部通过。
- 使用真实 CBZ/ZIP、CBR/RAR、CB7/7z、CBT/TAR 容器样本逐一读取，均识别为 3 页。
- 多压缩包严格按选择队列处理，包内按 `1、2、10` 自然排序，第二包从第一包末尾继续追加。
- 1000 页边界、原图 SHA-256、跨漫画物理去重、完整备份恢复、回收站恢复均通过。
- WebDAV 扫描、远程压缩包按需缓存、OPDS 自然排序与本机阅读进度通过。
- 已加入跨来源凭据测试：OPDS 外部下载地址不会收到书库 Basic/Digest 账号密码。

### Android 原位升级

- 在 Android 15 模拟器安装正式 `v1.0.0+1`，创建 `upgrade_probe` 并导入 1 张、775 KB 的真实图片。
- 未卸载、未清数据，使用 `adb install -r` 覆盖安装 `v1.1.0+2` 成功。
- 升级后漫画名、1 张图片、775 KB 统计与原图 SHA-256 全部不变。
- SQLite `user_version` 从 1 迁移为 3，并自动生成 57,344 字节的 `library.db.pre-v3` 安全快照。
- Release APK 再次覆盖安装成功；最终应用为非 Debug 构建，旧漫画和新增 CBZ 漫画都仍在书架。

### Android 功能回路

- 书架删除、移入回收站、回收站恢复已在模拟器实际操作，原图未被提前删除。
- 从系统文件选择器整包导入 `synthetic_comic.cbz`，自动建议名称并按原顺序建立 3 页漫画。
- 使用模拟器访问真实 HTTP OPDS 服务，成功挂载、扫描、下载、解压并打开 3 页远程漫画。
- 滚动后数据库只在本机记录阅读位置；解除挂载后本地索引与缓存为 0，服务器原 CBZ 保持不变。

### 最终安装包

- 包名：`com.shitmof.private_manga_reader`
- 版本：`1.1.0+2`
- 最低 Android SDK：24；目标 SDK：36。
- ABI：`arm64-v8a`、`armeabi-v7a`、`x86_64`。
- 大小：64,273,227 字节。
- SHA-256：`DE7FBE4DEB6BEB042E1634606F1F3FD6A77D3629372ED7709C3B8A59A1ACDF04`。
- APK Signature Scheme v2 校验通过。
- 签名证书 SHA-256 与 v1.0.0 完全一致：`3724690e26bae1597ad10221c10f2b2b15ade2a5bfdbaf408834dc46eaace1a5`。

## V1.0.0 基线验收

## 自动化

- Flutter 静态分析：零问题。
- 自动化测试：6 项全部通过。
- 真实生成 1000 个图片引用并执行反转重排；首尾顺序正确。
- 第 1001 张被产品上限拦截。
- 测试图片导入前后 SHA-256 一致。
- 删除测试源文件后，App 私有副本继续存在。
- 同一图片跨两本漫画只保留一个物理资源；删除一本中的引用不影响另一本。
- 完整备份恢复后，漫画名、封面、图片引用、顺序、阅读偏移和原图哈希一致。

## Android 15 实机回路

- Debug 与 Release APK 均构建成功并安装到 Android 15 模拟器。
- Release 版本号为 `1.0.0+1`，最终 APK 大小为 56,044,267 字节。
- 最终 APK SHA-256 为 `2AC9310AAF313FEBC7B0E03447F7F97025107E68ACAA74E26E0FA3AE5D01293C`。
- 冷启动成功，运行日志无 Flutter 崩溃。
- 新建漫画、系统文件选择、导入容量确认、原图导入、编辑页、详情页、阅读页、设置页和备份导出均已实际操作。
- Android 可用空间读取成功，导入确认页显示预计体积与设备可用空间。
- 空书架不显示虚假漫画或演示封面。
- 验收结束后已清理模拟器中的测试漫画，Release 冷启动显示真实空书架。

## 当前平台边界

- Android 已完成构建与实机验证。
- iOS 工程和相册权限说明已生成，但当前 Windows 主机无法执行 Xcode 构建与真机验收。
- HEIC/HEIF 原图会保持原始字节；若纯 Dart 缩略图解码器不支持某个厂商变体，原图仍可导入，卡片会回退到系统运行时解码。

## 编辑页回归修复与网络挂载复验（2026-08-30）

### 回归修复：编辑图片顺序“选中→删除→保存”

- 根因：`LibraryRepository.loadItems()` 返回固定长度列表，编辑页直接持有后，删除（`removeWhere`）与拖动（`removeAt`/`insert`）会抛 `Cannot remove from a fixed-length list`，表现为手机上“选中后无法删除/保存”。
- 修复：进入编辑页时复制为可变草稿列表（`List<ComicItemRecord>.of(items)`）；删除/拖动只改内存草稿，只有点“保存”才经 `applyItemEdits` 落库，取消不改变原图。
- 界面仅补既有编辑能力的可达性与反馈，不改变 UI 风格：整卡可点（`HitTestBehavior.opaque` + 语义按钮）、标题栏显示“已选择 N 张”、删除前弹确认。
- 自动化：`flutter analyze` 零问题；`flutter test` 18 项全部通过（原 13 项 + 编辑页 5 项回归：单选删除保存、批量选择删除保存、取消不变原图、保存后重进持久、长按拖动排序不崩溃且生效）。
- Android 模拟器实机回路（Debug APK，Android 15，1080×2400）：
  - 通过系统文件选择器逐张导入 3 张测试图（红/绿/蓝）→ 编辑页显示 3 个瓦片。
  - 点击第 2 张（绿）→ 出现蓝色选中边框与“已选择 1 张”→ 删除 → 红色确认按钮 → 确认 → 剩 2 张。
  - 保存 → 返回详情页；数据库剩 `test-1.png`、`test-3.png`；重新进入编辑页仍显示 2 张。
  - 批量选择 2 张 → “已选择 2 张”→ 删除确认 → 0/1000；点“取消”退出后数据库仍 2 张（草稿不落库）。
  - 长按拖动第 2 张到第 1 位 → 不崩溃 → 保存后数据库顺序变为 `[test-3, test-1]`。

### 网络挂载复验（本地真实 HTTP OPDS 服务）

- 本机 Python 起 OPDS 服务（`http://127.0.0.1:18080/opds.xml`），模拟器经 `10.0.2.2` 访问。
- 添加 OPDS 书库 → 验证连接 → 扫描发现 1 本远程漫画（3 页 CBZ）并入库。
- 打开远程漫画：下载 CBZ → 解压 → 3 页索引入库、`network-cache/` 缓存文件落盘；真实页面正常显示（红页滚动至蓝页）。
- 阅读进度只写本机：退出后 `last_read_position=1`。
- 解除挂载：`network_sources`/`remote_books`/`remote_pages` 清零，`network-cache/` 清空；服务器端 CBZ 保持原样（6413 字节不变，只读）。

### 发现与边界（未改动，待你决定）

- 系统文件选择器当前为单选：`ImportService.pickFromFiles()/pickFromGallery()` 未设置 `allowMultiple: true`，与 README“批量导入”描述不一致；本次验证以逐张导入完成，未改该行为（不在批准范围内）。若需要批量导入，建议后续补 `allowMultiple: true` 并加对应测试。
- 本次未 bump 版本号、未构建/发布新 APK；源码与文档已推送私有 GitHub（`main`），回退点标签 `backup-pre-editor-fix-20260830` 已推送。
