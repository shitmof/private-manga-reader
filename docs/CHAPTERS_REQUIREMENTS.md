# 拾画阁 · 章节与目录需求文档（待确认，未动工）

> 状态：**仅需求梳理，尚未修改任何代码**。
> 用途：请核对我的理解是否准确，并回答第八节的待定问题。
>
> **关于导入结果的表述（已按审查意见修正）**：
> 用户反馈实际**只导入了一部分**（7 本里只成功 1 本）。
> **程序识别出七个包，不代表七个包全部导入成功**——这两件事必须分开说。
> 我已修复一个临时文件清理缺陷，但**它是否解释用户此次失败，尚未证实**，
> 因为该 App 是 release 构建、非 debuggable、无 root，我读不到它的私有数据库，
> 无法自行核实导入结果。此前我写下的“识别全部成功、导入中断于第 2 本”属推断，已撤回。

---

## 一、你的想法（我的转述）

你希望一本漫画不再是一条平铺的图片列表，而是**可编排的章节结构**，并在导入时就能决定怎么分：

1. **一本书可以有目录**：由若干章节组成，能看目录、能跳章节。
2. **导入时选模式**：
   - **分段导入** —— 一个内层压缩包 = 一个章节（7 个 cbz → 第 1 章到第 7 章）。
   - **整段导入** —— 全部内容作为一整章（183 张就是一章）。
3. **在整段的基础上还能再切**：比如 160 张，可以
   - 按固定张数自动分（每 8 张一章 → 20 章）；
   - 按你自己指定的范围分（自定义每章起止）；
   - 或者不切，160 张当一章看。

如果这三条我理解偏了，请直接指出哪一条。

---

## 二、当前代码的现状（已核对，非推测）

| 项 | 现状 | 结论 |
|---|---|---|
| `comics` 表 | 有 `id/title/cover_asset_id/folder_id/...` | 无章节字段 |
| `comic_items` 表 | 只有 `position`（全书唯一顺序） | **扁平，无章节归属** |
| 章节 / 卷 / 目录表 | **不存在** | 需新增 |
| 多包导入流程 | `import_flow.dart:97` 只 `createComic()` **一次**，多个压缩包的图片导入**同一个 comicId** | **已有多包合并到一本书的流程** |
| 内层包导入 | 已支持（本轮修复） | 内层包被扫描为 selection 的多个条目，但**合并进同一本书** |
| 阅读器 | 单一连续列表 + 页码胶囊 | 无章节导航 |

> ⚠️ **本节曾写错并已修正**：此前写“每个内层包被建为独立的一本书 / 7 个 cbz 会变成 7 本书”，
> 与代码不符。经审查指出并复核 `lib/widgets/import_flow.dart:97` 确认：
> **当前是“一本漫画 + 多包图片合并”的流程**。

**这意味着两件事：**

- 这是**数据模型变更**（要动数据库结构），不是纯界面改动。
- 当前行为与你的目标**不一致**，但正确的表述是：
  **已有多包图片合并到一本书的流程，却没有保留“一包一章”的章节结构、目录与章节跳转。**
  （此前误写为“7 个 cbz 会变成 7 本书”，已更正。这个错误会直接误导方案设计，
  因为“合并到一本”恰恰说明**改造起点比原先设想的更近**——不需要先解决“多本书合并”，
  只需要给已有的单本多包结构补上章节归属。）

> ⚠️ 数据库结构变更必须配套升级与回退处理。当前代码**没有 `onDowngrade`**。
> 但要注意：**给新版本补上 `onDowngrade`，并不等于已发布的旧 APK 就能安全读取新数据库。**
> 旧 APK 不知道新表/新列的存在，需要单独设计「降级拒绝启动」「迁移前自动备份」
> 与「兼容读取」策略，不能靠新版本里的一个回调解决。

> 📌 另需注意（审查指出）：现有**书签保存的是 `item_id`，是稳定的图片标识**，
> 不是页码。章节划分与跨章节编辑**必须保留该标识的稳定性**，
> 不能按“书签记全书位置即可”处理——那样会让既有书签失效。

---

## 三、我建议的功能设计（供你挑选，不是最终方案）

### 3.1 数据层

新增“章节”概念，二选一：

- **方案 A：新增 `comic_chapters` 表 + 页码范围映射**
  字段：`id / comic_id / title / sort_index / start_position / page_count`
  `comic_items` 保持不变，章节通过页码范围推导。
  优点：改动最小。
  ⚠️ **缺点（审查指出，需重新评估）**：仅用“起始页 + 页数”表示章节，
  遇到**删除、插入、跨章节移动**时极易出错——页码是派生值，不是稳定标识。
  若采用此方案，必须明确“章节边界在编辑后如何重算”，并为每次编辑补测试。

- **方案 B：稳定章节 ID + 图片归属关系**
  给章节独立 ID，并让每张图片显式归属某个章节（如同现有书签用的 `item_id` 思路）。
  优点：章节是稳定标识，编辑、跨章节移动、书签、备份都更可靠。
  缺点：要迁移历史数据，编辑页、导出、阅读器、备份都要跟着改。

> **审查建议：优先评估方案 B。** 我原先推荐方案 A 的理由是“改动最小”，
> 但那低估了编辑场景下的边界重算风险。**在编辑功能是核心能力的前提下，
> 稳定性应优先于改动量。** 这一条我接受，方案 A/B 的推荐顺序应反转，
> 但最终取舍需要你决定（见第八节问题 1）。

### 3.2 导入流程（你关心的重点）

导入漫画压缩包时，在现有“确认导入”对话框上增加**分段方式**选择：

```
导入为新漫画

漫画名称   [________________]

分段方式
  ( ) 整段导入        全部内容作为一章
  ( ) 分段导入        每个压缩包作为一章         ← 内层包存在时才可选
  ( ) 自定义切分      按张数或指定范围分章

[当选择“自定义切分”时展开]
  方式  ( ) 每 [ 8 ] 张一章      ( ) 手动指定范围
  预计  20 章
  预览  第1章 1–8 · 第2章 9–16 · 第3章 17–24 …
```

要点：

- **只在有内层包时才显示“分段导入”**；单个 cbz 里只有图片时，
  这一项没有意义，不显示比显示成灰色更清楚。
- **默认值**要给一个明确选择，我倾向默认“分段导入”（有内层包时），
  因为你的原始诉求就是这个。
- **自定义切分要能预设章节名**：至少支持“第 N 章”自动编号，
  是否支持导入后改名见第八节。

### 3.3 阅读与目录

- 阅读器增加**章节目录**入口（顶部菜单里），列出章节名与页码范围，点击跳转。
- 阅读器底部页码胶囊的语义要明确：显示**全书页码**还是**本章页码**。
  我倾向“本章 3/16 · 全书 27/183”这种双段式，避免用户混淆。
- 上一章 / 下一章切换。

### 3.4 与现有功能的关系

| 现有功能 | 章节引入后的影响 |
|---|---|
| 图片编辑（排序/删除/移动） | 章节边界需随编辑重算；这是最容易出错的地方 |
| 导出 CBZ | 是否按章分文件？见第八节 |
| 书签 | 书签保存的是 **`item_id`（稳定图片标识）**，不是页码。章节划分与跨章节编辑**不得破坏该标识稳定性**，否则既有书签会失效 |
| 阅读进度 | 记全书位置；目录里可显示“读到本章第几页” |
| 备份 / 恢复 | 章节信息必须进备份，否则恢复后目录丢失 |
| 私密书架 | 章节不改变私密语义，无需改 |

---

## 四、明确不做的事（避免范围失控）

- 不做多级目录（章下面再分节）。只做“一本漫画 → 若干章”这一层。
- 不改变现有扁平数据的行为，除章节外不动排序、归属、私密逻辑。
- 不为章节单独引入新的一级导航，仍从漫画详情页进入。
- 不把独立“书单”功能与章节合并（书单是跨漫画的集合，语义不同）。

---

## 五、风险与代价（我先说清楚）

1. **数据库迁移是硬成本**。要么加表（方案 A，风险低），要么改 `comic_items`（方案 B，风险高）。
   无论哪种，**都必须同时补上 `onDowngrade` 与迁移前自动备份**。
2. **图片编辑是最大的回归风险**。现有编辑页支持重排、批量移动、按范围选择，
   引入章节后每一次编辑都可能改变章节边界。这一块必须有针对性测试。
3. **备份格式要升版本**。旧备份导入时必须能兼容（按“整本一章”处理）。
4. **已有用户数据的处理**：现有漫画没有章节，迁移时统一视为“整本一章”，
   这样目录里只有一章，不破坏原有阅读体验。

---

## 六、实施顺序建议

```
1. 补数据库降级保护与迁移前备份        ← 前置，否则后面都是风险
2. 数据层加章节（方案 A）
3. 导入流程增加分段方式与自定义切分
4. 阅读器章节目录与跳转
5. 编辑页适配（章节边界重算）
6. 导出 / 备份 / 恢复适配
7. 回归测试与真实设备验收
```

---

## 七、验收标准（我想按这个验，你看是否同意）

- 导入 7 个 cbz 的嵌套包，选“分段导入”后得到**1 本书、7 章**，章名可辨。
- 同样内容选“整段导入”，得到**1 本书、1 章、183 张**。
- 160 张选“每 8 张一章”，得到 20 章，且每章页码范围正确。
- 自定义切分能指定范围，边界与预览一致。
- 目录能跳章，页码显示不混淆。
- 旧数据（无章节）升级后不丢、不崩，显示为整本一章。
- 备份 → 恢复后章节结构完整。
- 编辑页删除/移动图片后，章节边界正确且不越界。

---

## 八、需要你决定的问题

| # | 问题 | 我的倾向 |
|---|---|---|
| 1 | 数据层用**方案 A（加表）**还是**方案 B（改 comic_items）**？ | A，风险低 |
| 2 | 有内层包时，导入默认选**分段**还是**整段**？ | 分段 |
| 3 | 章节名默认怎么取？用内层包文件名（如“连载_第1话 （31P）”），还是统一“第 N 章”？ | 用文件名，可改 |
| 4 | 自定义切分的**每章张数**，上限要不要限制？ | 不限制，但超过全书张数时提示 |
| 5 | 导入后**还能不能改章节划分**？（合并章、拆分章、重命名） | 建议至少支持重命名；合并/拆分可作为后续 |
| 6 | 阅读器页码显示**全书页码**还是**本章 + 全书**？ | 本章 + 全书 |
| 7 | 导出 CBZ 时**按章分多个文件**还是合成一个？ | 默认合成一个，另提供按章导出 |
| 8 | 现有已导入的漫画，要不要提供“把多本书合并成一本并分章”的工具？ | 建议做，否则你手上那 1 本要重导 |
| 9 | 这次要不要**同时**把第 3 节的导入界面改动一起做，还是先只做数据层？ | 一起做，否则功能不可见 |

---

## 九、附：与本任务书其他部分的关系

- 这不属于原《整体 UI 升级、功能补齐与版本验收任务》的既有条目，
  属于**新增需求**。它会排在 7.2 压缩包导入之后、7.4 存储缓存之前。
- 它会推迟 v1.6.2 的发布，或作为 v1.7.0 单独发布。建议**单独一版**，
  因为它是数据模型变更，与视觉/交互改动混在一起不好回退。
